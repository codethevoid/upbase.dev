export const GodRays = () => {
  // if (process.env.NODE_ENV === "development") {
  //   return null;
  // }

  return (
    <div className="to-background absolute inset-0 z-[-1] overflow-hidden">
      <div className="rainbownight absolute -inset-[10px] opacity-50"></div>
    </div>
  );
};
